{
  "name": "traefik-ui",
  "version": "0.6.5",
  "description": "Web UI for managing Traefik proxy configurations with DNS provider management and CrowdSec integration",
  "main": "deploy/app/server.js",
  "scripts": {
    "start": "cd deploy/app && node server.js",
    "dev": "cd deploy/app && nodemon server.js",
    "deploy": "cd deploy && ./setup.sh"
  },
  "dependencies": {
    "express": "^4.18.2",
    "multer": "^1.4.5-lts.1",
    "js-yaml": "^4.1.0"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  },
  "keywords": [
    "traefik",
    "proxy",
    "reverse-proxy",
    "docker",
    "letsencrypt",
    "dns-challenge",
    "rfc2136",
    "crowdsec",
    "web-ui",
    "configuration-management"
  ],
  "repository": {
    "type": "git",
    "url": "https://github.com/nulluserid/traefik-ui.git"
  },
  "license": "MIT",
  "engines": {
    "node": ">=18.0.0"
  }
}